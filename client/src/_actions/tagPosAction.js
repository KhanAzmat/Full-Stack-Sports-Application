/*import * as types from "./types";

//Get all Tags
export const tagPos = (payload) => async (dispatch) => {
  try {
    dispatch({
      type: types.GET_TAG_POSES,
      payload: payload,
    });
  } catch (err) {
    dispatch({
      type: types.TAG_POS_ERROR,
      payload: { status: err.response },
    });
  }
};*/


